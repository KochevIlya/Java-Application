package com.example.javaapplication;import com.vaadin.flow.spring.annotation.EnableVaadin;import org.springframework.boot.SpringApplication;import org.springframework.boot.autoconfigure.SpringBootApplication;import org.springframework.context.ApplicationContext;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.util.Objects;@SpringBootApplication@EnableVaadinpublic class Main {    public static void main(String[] args) {        System.out.println("Добрый день, сэр! Готов к работе, сэр");        char ch = 0;        do {            System.out.println("Введите 1 если хотите, чтобы запустился GUI или 2, если хотите, чтобы запустился Web: ");            InputStreamReader isr = new InputStreamReader(System.in);            BufferedReader br = new BufferedReader(isr);            String answ;            try {                answ = br.readLine();            }            catch(Exception e)            {                System.out.println("Вы ввели неверный символ(\n Попробуйте заново!");                ch = '1';                continue;            }            if (Objects.equals(answ, "1")) {               GUI gui = new GUI();            }            else if (Objects.equals(answ, "2")) {                ApplicationContext applicationContext = SpringApplication.run(Main.class, args);                System.out.println("Приложение запустилось, теперь перейдите в браузер и откройте его в порту 8080)");                System.out.println("Введите 1, если хотите закрыть web:");                try {                    answ = br.readLine();                    if (Objects.equals(answ, "1"))                        SpringApplication.exit(applicationContext);                }                catch(Exception e)                {                    System.out.println("Вы ввели некорректный символ(");                    SpringApplication.exit(null);                }            }            else {//                CLI cli = new CLI();//                cli.activate();            }            System.out.println("Введите 1, если хотите запустить программу заново: ");            try {                answ = br.readLine();                ch = answ.charAt(0);            }            catch (IOException e) {                System.out.println("Вы ввели неверный ответ, попробуйте снова!");                ch = '1';                continue;            }        }        while(ch == '1');        System.exit(0);    }}